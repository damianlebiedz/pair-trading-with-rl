# yaml-language-server: $schema=schemas/schema.json

fixed_window: 168
entry_threshold: 3.0
exit_threshold: 0.0
stop_loss: 2.0

generate_plots: false

pair_selection:
  top_n_factor: 20
  coint_type: eg

  start: "2023-11-01"
  end: "2024-01-01"

performance:
  use_rl: false
  model_name:

  iterations: 12

  beta_hedge: rolling
  window_method: fixed

  delayed_entry: true
  sl_lock: true
  time_decay_sl: true

  test:
    beta_start: "2023-12-01"
    win_start: "2023-12-01"
    start: "2024-01-01"
    end: "2024-02-01"

defaults:
  - _self_
  - base
